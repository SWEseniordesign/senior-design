import { Typography } from "@mui/material";
import React from "react";

export const AccessBusiness = () => {
    return (
        <div>
            <Typography>Access Business</Typography>
        </div>
    );
}